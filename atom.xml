<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

    <title>Blades</title>
    <link rel="related" href="" />
    <id></id>
    
    <author>
        <name>Maros Grego</name>
        
        
    </author>
    
    <subtitle></subtitle>
    <generator>blades</generator>
    <updated>2021-12-22T16:04:59Z</updated>


    <entry>
        <title>Index</title>
        <link rel="alternate" href="/" />
        <id></id>
        <summary></summary>
        <content><code class="highlighted">
<p><strong>bla</strong>zing fast
<strong>de</strong>ad simple
<strong>s</strong>tatic site generator</p>
</code>
<p>Blades is made to do one job - generate HTML files from the provided
content using the provided templates.<br />
Thanks to <a href="https://github.com/maciejhirsz/ramhorns">Ramhorns</a> templating engine,
<a href="https://serde.rs/lifetimes.html#borrowing-data-in-a-derived-impl">zero-copy</a> deserialisation
and <a href="https://github.com/rayon-rs/rayon">rayon</a> parallel iterators, it renders the whole site in
milliseconds, possibly more than <a href="https://github.com/grego/ssg-bench">10 times</a> faster than other generators like Hugo.</p>
<p>It’s made for easy setup and use. A static site generator should be a no brainer.
It uses <a href="https://mustache.github.io/mustache.5.html">mustache</a> templates with extremely minimal
and obvious syntax (like 7 rules!), providing the necessary building blocks
to let you focus on your content.</p>
<h2>Features</h2>
<ul>
<li>Powerful plugin system</li>
<li>Themes</li>
<li>Image gallery generation</li>
<li><a href="https://commonmark.org">CommonMark</a> markdown with tables and footnotes for content</li>
<li>Automatic syntax highlighting using <a href="https://github.com/grego/cmark-syntax">cmark-syntax</a>
(with a possibility of turning LaTeX formulas into <a href="https://developer.mozilla.org/docs/Web/MathML">MathML</a>)</li>
<li>Customisable taxonomies (like categories or tags)</li>
<li>Pagination</li>
<li>Breadcrumbs</li>
<li>Asset colocation</li>
<li>Table of contents with access to all of the site data</li>
<li>Automatic sitemap, Atom and RSS feed generation</li>
</ul>
<h2>Why not <em><code>blades</code></em>?</h2>
<p>Unlike other monolithic generators, Blades is modest in its scope. All it does is generating your site.
It doesn’t do any fancy stuff like transpiling Haskell to minified Javascript, or ever
watching the site for changes. For that, you can use a dedicated tool like
<a href="https://github.com/grego/caretaker">caretaker</a>.</p>
<p>Nevertheless, if you have a feature request or ran into some issue using Blades, please submit an
<a href="https://github.com/grego/blades">issue</a>. It is a hobby project, so any contribution
is welcome! <code>:)</code></p>
<h2>Why <em><code>blades</code></em>?</h2>
<p>They shave the <a href="https://mustache.github.io/mustache.5.html">mustache</a> off.</p>
</content>
        
    </entry>
    

    <entry>
        <title>Getting started</title>
        <link rel="alternate" href="/getting_started.html" />
        <id>/getting_started</id>
        <summary></summary>
        <content><p>If you have the Rust toolchain installed, you can install Blades from <a href="https://crates.io/crates/blades">crates.io</a></p>
<pre><code class="language-sh"><strong>cargo</strong> install blades
</code></pre>
<p>It is also available on <a href="https://aur.archlinux.org/packages/blades-bin/">AUR</a>.</p>
<p>Then, you can run the executable <code>blades</code> with the following subcommands:</p>
<div id="commands">
<p><code>init</code> Initialise the site in the current directory, creating the basic files and folders</p>
<p><code>build</code> Build the site according to config, content, templates and themes in the current directory</p>
<p><code>colocate</code> Move the assets from the “assets” directory and from the theme, if one is used, into the output directory</p>
<p><code>all</code> Build the site and colocate the assets</p>
<p><code>lazy</code> Build the site and (colocate assets only if the theme was switched) [default]</p>
<p><code>new</code> Create a new page</p>
</div>
<h2>Themes</h2>
<p>When you specify a theme in the <a href="config.html">config</a>, templates and assets from the theme are used.
Every site that doesn’t use a theme can be used as a theme for another site.
To use it, simply clone it into the themes directory.</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> clone <var>$site_repository</var>
</code></pre>
<p>Then, set it as a theme in <code>Blades.toml</code>:</p>
<pre><code class="language-toml"><var>theme</var> = <span>&quot;$site_name&quot;</span>
</code></pre>
<p>To overwrite the theme, simply use the files in the <code>templates</code>, resp. <code>assets</code> subdirectories of the
page root directory.</p>
<h2>Assets</h2>
<p>All the files from the <code>assets</code> directory (and from the theme) are moved into the directory
specified in the <a href="config.html">config</a>, which is emptied before. This is a subdirectory of the
output directory (defaults to <code>assets</code>).</p>
<p>Blades takes of the pages it rendered before and if some of them is deleted, the corresponding
files in the output directory will be deleted, too. The other files in the output directory
are left intact. This way, you can place anything in the output directory and (as long as its name
differs from all the page names and it’s not in the assets subdirectory), Blades won’t touch it.</p>
<h2>Meta</h2>
<p>Blades renders <a href="https://www.sitemaps.org">sitemap</a> (into <code>sitemap.xml</code>), <a href="https://en.wikipedia.org/wiki/Atom_(Web_standard)">Atom</a> (into <code>atom.xml</code>)
and <a href="https://en.wikipedia.org/wiki/RSS">RSS</a> (into <code>rss.xml</code>) feeds, unless explicitly disabled in the <a href="config.html">config</a>.</p>
<h2>Contribution</h2>
<p>If you found a bug or would like to see some feature in Blades, you are the most welcome to submit an issue
or a pull request! Likewise if you found something in this documentation not clear or imprecise.
“”“</p>
</content>
        
    </entry>
    

    <entry>
        <title>Configuration</title>
        <link rel="alternate" href="/config.html" />
        <id>/config</id>
        <summary></summary>
        <content><p>The site can be configured in the file <code>Blades.toml</code> in its root directory. There, the following
variables can be set. All of them are optional, default values are shown.</p>
<p><strong>Any</strong> other variable of <strong>any type</strong> can be set there and used in the templates, too.</p>
<pre><code class="language-toml"><i># Title of the site</i>
<var>title</var> = <span>&quot;&quot;</span>
<i># URL which the site is expected to be accessed through</i>
<var>url</var> = <span>&quot;&quot;</span>
<i># Name of the folder where theme templates and assets shall be loaded from,</i>
<i># located in the themes directory (see below)</i>
<var>theme</var> = <span>&quot;&quot;</span>
<var>description</var> = <span>&quot;&quot;</span> 
<var>keywords</var> = <span>&quot;&quot;</span>
<var>image</var> = <span>&quot;&quot;</span>

<i># Generate sitemap</i>
<var>sitemap</var> = <span>true</span>
<i># Generate RSS feed</i>
<var>rss</var> = <span>true</span>
<i># Generate Atom feed</i>
<var>atom</var> = <span>true</span>
<i># Generate taxonomies that were not explicitly defined in this config</i>
<var>implicit_taxonomies</var> = <span>true</span>
<i># When no date for a page is specified, use the file creation date</i>
<var>dates_of_creation</var> = <span>true</span>

<i># A directory where the content is loaded from</i>
<var>content_dir</var> = <span>&quot;content&quot;</span>
<i># A directory where the theme is loaded from</i>
<var>theme_dir</var> = <span>&quot;themes&quot;</span>
<i># A directory where the content is rendered to</i>
<var>output_dir</var> = <span>&quot;public&quot;</span>
<i># A subdirectory of the output directory where the assets are copied to</i>
<var>assets</var> = <span>&quot;assets&quot;</span>

<u>[</u><strong>author</strong><u>]</u>
<var>name</var> = <span>&quot;&quot;</span>
<var>uri</var> = <span>&quot;&quot;</span>
<var>email</var> = <span>&quot;&quot;</span>
<var>avatar</var> = <span>&quot;&quot;</span>

<i># A table of tables where you can specify some data about taxonomies</i>
<u>[</u><strong>taxonomies</strong><u>]</u>
<i># Example with default values:</i>
<var>tags</var> = <u>{</u> <var>name</var> = <span>&quot;&quot;</span>, <var>description</var> = <span>&quot;&quot;</span>, <var>template</var> = <span>&quot;taxonomy.html&quot;</span>, <var>key_template</var> = <span>&quot;taxonomy_key.html&quot;</span>, <var>paginate_by</var> = <span>inf</span>, <var>sort_by_weight</var> = <span>false</span> <u>}</u>
<i># If provided, 'paginate_by' must be a positive integer</i>

<i># Plugins to use in the site</i>
<u>[</u><strong>plugins</strong><u>]</u>
<i># Commands to run as input plugins</i>
<var>input</var> = <u>[]</u>
<i># Commands to run as output plugins</i>
<var>output</var> = <u>[]</u>
<i># Commands to run as transform plugins</i>
<var>transform</var> = <u>[]</u>
<i># A map of a short name and a command to run as a content plugin</i>
<i># Each one can then be enabled on a particular page to transform its content</i>
<var>content</var> = <u>{}</u>
<i># A list of content plugins (specified above) to run on every page</i>
<var>default</var> = <u>[]</u>
</code></pre></content>
        
    </entry>
    

    <entry>
        <title>Pages</title>
        <link rel="alternate" href="/pages.html" />
        <id>/pages</id>
        <summary></summary>
        <content><p>Blades loads all <a href="https://commonmark.org">Markdown</a>
files (<code>.md</code> extension) with an optional <a href="https://toml.io">TOML</a> header (delimited by <code>+++</code>) 
and all TOML files (<code>.toml</code> extension) from the directory
specified in the <a href="config.html">config</a> (defaults to <code>content</code>).</p>
<p>In these files, the page content
and metadata are provided. They are then rendered using the provided <a href="templates.html">templates</a>.</p>
<p>When the file is called <code>index.md</code> or <code>index.toml</code>, the corresponding page is called <strong>section</strong>. The rendered page (<code>index.html</code>) is displayed
when the user browses to the parent URL. They can use some additional data in templates,
such as their subpages and subsections. Other files are treated as standard pages.</p>
<p>In pages (either in the header or in the TOML files), these variables can be specified.
All of them are optional, default values are shown. <br>
<strong>Any</strong> other variable of <strong>any type</strong> can be set there and used in the templates, too.</p>
<pre><code class="language-toml"><i># Title of the page</i>
<var>title</var> = <span>&quot;&quot;</span>
<i># Date when the page was created</i>
<var>date</var> = <span>nan</span>
<i># date = 2020-09-08</i>

<i># An image representing the page</i>
<var>image</var> = <span>&quot;&quot;</span>
<i># A brief summary of the page content</i>
<var>summary</var> = <span>&quot;&quot;</span>

<i># The main content of the page</i>
<i># CommonMark markdown can be used here</i>
<i># In Markdown files, it is replaced with the actual content of the file, if present.</i>
<var>content</var> = <span>&quot;&quot;&quot;
&quot;&quot;&quot;</span>

<i># A weight of the page, used if a collection this page is in is sorted by weight</i>
<var>weight</var> = <span>0</span>
<i># A priority of this page for SEO, in the range of 0-1</i>
<var>priority</var> = <span>0.5</span>
<i># Hide the page from the list of its parent's subpages or subsections</i>
<var>hidden</var> = <span>false</span>
<i>#A list of alternative paths to render this page in, relative to the output directory</i>
<var>alternative_paths</var> = <u>[]</u>

<i>#A list of content plugins to transform the content of this page with</i>
<var>plugins</var> = <u>[]</u>

<i># A template to render this page with.</i>
<i># If none is specified, the one specified in the parent section is used</i>
<var>template</var> = <span>&quot;&quot;</span>
<i># A template to render the gallery pictures with</i>
<var>gallery_template</var> = <span>&quot;gallery.html&quot;</span>

<i># A list of pictures associated with this page</i>
<i># Each will be rendered into a separate gallery page</i>
<u>[[</u><strong>pictures</strong><u>]]</u>
<i># Id string of the picture, used for the generated URL in the gallery page</i>
<i># MANDATORY for every picture used!</i>
<var>pid</var> = <span>&quot;&quot;</span>
<i># An associated caption of the picture</i>
<i># [CommonMark](https://commonmark.org/) markdown can be used here</i>
<var>caption</var> = <span>&quot;&quot;</span>
<i># An alternative text displayed when the image can't be loaded of for accessibility</i>
<var>alt</var> = <span>&quot;&quot;</span>
<i># File name of the image</i>
<var>file</var> = <span>&quot;&quot;</span>
<i># Date and time of when the image was taken</i>
<var>taken</var> = <em>None</em>
<i># Repeat for every picture</i>

<i># A map of lists to classify this page with</i>
<u>[</u><strong>taxonomies</strong><u>]</u>
<i># For example:</i>
<i># tags = [&quot;blades&quot;, &quot;documentation&quot;]</i>
</code></pre>
<h2>Sections</h2>
<p>For pages that are sections, you can additionally specify these variables:</p>
<pre><code class="language-toml"><i># A number of pages to paginate by, if this number is exceeded</i>
<i># If provided, must be a positive integer</i>
<var>paginate_by</var> = <span>inf</span>
<i># A template to render every subpage with (unless it specifies another template)</i>
<var>page_template</var> = <span>&quot;page.html&quot;</span>
<i># A template to render every subsection with (unless it specifies another template)</i>
<var>section_template</var> = <span>&quot;section.html&quot;</span>
<i># Sort subpages and subsetions by their provided weight, low to high</i>
<i># By default, they are sorted by their date, newer to older</i>
<var>sort_by_weight</var> = <span>false</span>
</code></pre>
<h2>Not sections</h2>
<p>For pages that are not sections, you can additionally specify these variables
(defaults are suggested):</p>
<pre><code class="language-toml"><i># The path in the output directory this page is rendered into*</i>
<var>path</var> = <span>&quot;{{ parent directory of the page }}&quot;</span>
<i># The file name this page is rendered into, without the .html extension*</i>
<var>slug</var> = <span>&quot;{{ file name without the extension }}&quot;</span>
</code></pre></content>
        
    </entry>
    

    <entry>
        <title>Templates</title>
        <link rel="alternate" href="/templates.html" />
        <id>/templates</id>
        <summary></summary>
        <content><p>Blades uses <a href="https://mustache.github.io/mustache.5.html">mustache</a> templates with the 
<a href="https://github.com/maciejhirsz/ramhorns">Ramhorns</a> engine. While their syntax is simple
(and can be learned in a few minutes), they are surprisingly expressive.</p>
<p>Templates are loaded from the <code>templates</code> subdirectory, or secondarily from the <code>themes/$theme/templates</code>
directory if a theme is specified in the <a href="config.html">config</a>.</p>
<p>Here is the list of variables available to use in templates. Apart from these,
<strong>any</strong> other variable of <strong>any type</strong> specified on the <a href="/page.html">page</a>
or in the <a href="/config.html">site config</a> can also be used.</p>
<p>These variables are common for all pages:</p>
<code class="highlighted">
<p><em>// The data of the whole site</em>
<strong>site</strong>: <a href="#site">Site</a>
<em>// Main page in the site</em>
<strong>index</strong>: <a href="#page">Page</a>
<em>// A dictionary of all the taxonomies used in the site</em>
<strong>classification</strong>: Map&lt;string, <a href="#taxonomy">Taxonomy</a>&gt;</p>
</code>
<h2>Page</h2>
<p>When rendering a page, these variables are available:</p>
<code id="page" class="highlighted">
<p><em>// Title of the page</em>
<strong>title</strong>: string
<em>// Date when the page was created</em>
<strong>date</strong>: Option&lt;<a href="#datetime">DateTime</a>&gt;
<em>// Image representing the page</em>
<strong>image</strong>: string
<em>// A brief summary of the page</em>
<strong>summary</strong>: string
<em>// The main content of the page</em>
<strong>content</strong>: string</p>
<p><em>// The full link of the page, with the site URL included</em>
<strong>permalink</strong>: string
<em>// The path of the page, beginning with <code>/</code>, without the last segment</em>
<em>// When rendered as section ({{#path}}), it acts as a list of <a href="#ancestors">Ancestors</a></em>
<strong>path</strong>: string
<em>// The trailing segment of this page’s URL</em>
<em>// Without the .html extension</em>
<em>// For sections, it’s the folder name rather than <code>index</code></em>
<strong>slug</strong>: string
<em>// A list of alternative paths where this page is rendered to, if any</em>
<strong>alternative_paths</strong>: List<string></p>
<p><em>// A list of classifications for each of the provided taxonomies</em>
<strong>taxonomies</strong>: Map&lt;string, List&lt;string&gt;&gt;
<em>// A list of pictures provided by this page</em>
<strong>pictures</strong>: List&lt;<a href="#picture">Picture</a>&gt;</p>
<p><em>// Is this page a section?</em>
<strong>is_section</strong>: bool
<em>// Is the page hidden from the list of its parent’s subpages and subsections?</em>
<strong>hidden</strong>: bool
<em>// A priority of this page for SEO, in the range of 0-1</em>
<strong>priority</strong>: float</p>
<p><em>// The page one level up in the hierarchy (for the index page it’s itself)</em>
<strong>parent</strong>: <a href="#page">Page</a>
<em>// The previous page on this level, if any</em>
<strong>previous</strong>: Option&lt;<a href="#page">Page</a>&gt;
<em>// The next page in this level, if any</em>
<strong>next</strong>: Option&lt;<a href="#page">Page</a>&gt;
<em>// A list of all the subpages (empty if not section)</em>
<strong>pages</strong>: List&lt;<a href="#page">Page</a>&gt;
<em>// A list of all the subsections (empty if not section)</em>
<strong>subsections</strong>: List&lt;<a href="#page">Page</a>&gt;
<em>// Available when the subpages are paginated</em>
<strong>pagination</strong>: Option&lt;<a href="#pagination">Pagination</a>&gt;</p>
<p><em>// Marks whether this page is the active one</em>
<em>// Can be used to highlight the current page in a list of pages</em>
<strong>active</strong>: bool</p>
</code>
<h3>Pagination</h3>
<p>When the pagination is available, you can use these variables in the pagination section
(<code>{{#pagination}} ... {{/pagination}}</code>):</p>
<code id="pagination" class="highlighted">
<p><em>// Number of the current page in the paginated pages</em>
<strong>current</strong>: integer
<em>// Number of the previous page in the paginated pages</em>
<em>// This is also its slug</em>
<strong>previous</strong>: Option&lt;integer&gt;
<em>// Number of the next page in the paginated pages</em>
<em>// This is also its slug</em>
<strong>next</strong>: Option&lt;integer&gt;
<em>// Number of the paginated pages</em>
<strong>length</strong>: integer</p>
</code>
<h3>Ancestors</h3>
<p>When the page path is used as a section (<code>{{#path}} ... {{/path}}</code>), it is interpreted
as a list of ancestors. This makes making breadcrumbs possible.
For the path segments, the following are available:</p>
<code id="ancestors" class="highlighted">
<p><em>// Name of the current path segment</em>
<strong>name</strong>: string
<em>// The full path up to this segment</em>
<strong>full</strong>: string</p>
</code>
<h3>DateTime</h3>
<p>When the date is available, you can use these variables in the datetime section
(<code>{{#date}} ... {{/date}}</code>), roughly corresponding to <a href="https://strftime.org/">strftime</a>:</p>
<code id="picture" class="highlighted">
<p><em>// Year</em>
<strong>y</strong>: integer
<em>// Month</em>
<strong>m</strong>: integer
<em>// Day, 0-padded</em>
<strong>d</strong>: integer
<em>// Day, space-padded</em>
<strong>e</strong>: integer
<em>// Hour</em>
<strong>H</strong>: integer
<em>// Minute</em>
<strong>M</strong>: integer
<em>// Second</em>
<strong>S</strong>: integer
<em>// First 3 letters of the English month name</em>
<strong>b</strong>: string
<em>// First 3 letters of the English weekday name</em>
<strong>a</strong>: string</p>
</code>
<h3>Picture</h3>
<p>When pictures are available, you can use these variables in the pictures section
(<code>{{#pictures}} ... {{/pictures}}</code> for a list of them):</p>
<code id="picture" class="highlighted">
<p><em># Id string of the picture, used for the generated URL in the gallery page</em>
<strong>pid</strong>: string
<em># An associated caption of the picture</em>
<strong>caption</strong>: string
<em># An alternative text displayed when the image can’t be loaded of for accessibility</em>
<strong>alt</strong>: string
<em># File name of the image</em>
<strong>file</strong>: string
<em># Date and time of when the image was taken</em>
<strong>taken</strong>: Option&lt;<a href="#datetime">DateTime</a>&gt;
<em>// The full link of the picture, with the site URL included</em>
<strong>permalink</strong>: string</p>
</code>
<h2>Gallery</h2>
<p>When a page contains come picture, the gallery is created. The page of each photo in the gallery
gets the following variables:</p>
<code id="gallery" class="highlighted">
<p><em>// The current picture</em>
<strong>current</strong>: <a href="#picture">Picture</a>
<em>// The previous picture in the list (the last one for the first one)</em>
<strong>previous</strong>: <a href="#picture">Picture</a>
<em>// The next picture in the list (the first one for the last one)</em>
<strong>next</strong>: <a href="#picture">Picture</a>
<em>// The parent page, from which this gallery is generated</em>
<strong>parent</strong>: <a href="#page">Page</a></p>
</code>
<h2>Taxonomy</h2>
<p>Each taxonomy that is rendered gets the following variables:</p>
<code id="taxonomy" class="highlighted">
<p><em>// Full name of the taxonomy</em>
<strong>title</strong>: string
<em>// A short name of the taxonomy, used in the URL</em>
<strong>slug</strong>: string
<em>// A brief description of the taxonomy</em>
<strong>description</strong>: string
<em>// A list of keys used in this taxonomy and their corresponding pages</em>
<strong>keys</strong>: List&lt;<a href="#keypages">KeyPages</a>&gt;</p>
</code>
<h3>Key pages</h3>
<p>When keys in the taxonomy are available, you can use these variables in the keys section
(<code>{{#keys}} ... {{/keys}}</code> for a list of them):</p>
<code id="keypages" class="highlighted">
<p><em>// Name of this key (also used in the URL of the key page)</em>
<strong>key</strong>: str
<em>// All the pages with this key in this taxonomy</em>
<strong>pages</strong>: List&lt;<a href="#page">Page</a>&gt;</p>
</code>
<h2>Taxonomy key</h2>
<p>When rendering a page of a single taxonomy key, these variables are available:</p>
<code id="taxonomy-key" class="highlighted">
<p><em>// Name of this key (also used in the URL)</em>
<strong>title</strong>: string
<em>// The parent taxonomy where this key belongs to</em>
<strong>taxonomy</strong>: <a href="#taxonomy">Taxonomy</a>
<em>// The pages using this key</em>
<strong>pages</strong>: List&lt;<a href="#page">Page</a>&gt;
<em>// Optional pagination, if enabled for this taxonomy</em>
<strong>pagination</strong>: Option&lt;<a href="#pagination">Pagination</a>&gt;</p>
</code>
<h2>Site</h2>
<p>The data of the whole site, available for every page in the site section
(<code>{{#site}} ... {{/site}}</code>):</p>
<code id="site" class="highlighted">
<p><em>// The main title of the site</em>
<strong>title</strong>: string
<em>// The main URL of the site</em>
<strong>url</strong>: string
<strong>description</strong>: string
<strong>keywords</strong>: string
<em>// A representative image of the site</em>
<strong>image</strong>: string
<em>// Info about the author of the site</em>
<strong>author</strong>: <a href="#author">Author</a></p>
<p><em>// Path of the site where the assets are moved to</em>
<strong>assets</strong>: string
<em>// Is sitemap rendered?</em>
<strong>sitemap</strong>: bool
<em>// Is atom feed rendered?</em>
<strong>atom</strong>: bool
<em>// Is RSS feed rendered?</em>
<strong>rss</strong>: bool
<em>// Are taxonomies not explicitly defined in the config, but used in some page rendered?</em>
<strong>implicit_taxonomies</strong>: bool
<em>// Are pages without date provided assigned the date of file creation?</em>
<strong>dates_of_creation</strong>: bool</p>
</code>
<h3>Author</h3>
<p>Info about the site author, available as a subsection of <a href="#site">site</a>
(<code>{{#site}}{{#author}} ... {{/author}}{{/site}}</code>):</p>
<code id="author" class="highlighted">
<p><strong>name</strong>: string
<strong>uri</strong>: string
<strong>email</strong>: string
<strong>avatar</strong>: string</p>
</code>
</content>
        
    </entry>
    

    <entry>
        <title>Making plugins</title>
        <link rel="alternate" href="/making-plugins.html" />
        <id>/making-plugins</id>
        <summary></summary>
        <content><p>Blades has a versatile extension system, where a plugin can be written in any language
that can read from standard input and write to the standard output.
Each plugin is run at the appropriate stage of site rendering.</p>
<p>Check out the already made <a href="/plugins/index.html">plugins</a>.</p>
<p>There are 4 types of plugins that can be used with Blades.</p>
<ul>
<li><strong>input</strong> - they put a JSON-serialised list of <a href="/pages.html">pages</a> on the standard output, can be used
to get pages from different sources</li>
<li><strong>output</strong> - they receive a JSON-serialised list of <a href="/pages.html">pages</a> on the standard input and can be
used to generate further page data, such as processed images</li>
<li><strong>transform</strong> - they receive a JSON-serialised list of <a href="/pages.html">pages</a> on the standard input and output
another such list on the standard output, can transform anything on the pages</li>
<li><strong>content</strong> - they receive a markdown content of one page on standard input and output markdown on the standard output; they are enabled
on per-page basis</li>
</ul>
<p>The plugins can be set up in the <a href="/config.html">config</a>.</p>
<h3>Serialised page data</h3>
<p>All the data specified for every <a href="/pages.html">page</a> is available in the serialised list of pages.
It contains only the fields that were explicitly set, so no field can be assumed to be
present or non-present.
For Rust, Blades also provides a
<a href="https://docs.rs/blades">library</a> for automatic serialisation and deserialisation of pages.</p>
<h3>Example</h3>
<p>An example plugin can be found in the <a href="https://github.com/grego/blades">Blades repository</a>
in the <code>examples</code> directory.
To try it, clone it and downolad the theme as a submodule</p>
<pre><code class="language-bash"><strong>git</strong> clone https://github.com/grego/blades
<strong>cd</strong> blades
<strong>git</strong> submodule update --init
</code></pre>
<p>Then build the plugin:</p>
<pre><code class="language-bash"><strong>cargo</strong> build --release transform_plugin
</code></pre>
<p>Then run Blades in the <code>examples</code> directory:</p>
<pre><code class="language-bash"><strong>cd</strong> examples
<strong>cargo</strong> run --release
</code></pre>
<p>Or check some <a href="/plugins/index.html">real plugins</a>.</p>
</content>
        
    </entry>
    

    <entry>
        <title>Themes</title>
        <link rel="alternate" href="/themes/" />
        <id>/themes</id>
        <summary></summary>
        <content><p>Any site that doesn’t use a theme can be used as a theme for another site.</p>
<p>To use it, simply clone it into the themes directory.</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> clone <var>$site_repository</var>
</code></pre>
<p>Then, set it as a theme in <code>Blades.toml</code>:</p>
<pre><code class="language-toml"><var>theme</var> = <span>&quot;$site_name&quot;</span>
</code></pre>
<p>To contribute your own theme, submit a PR to the <a href="https://github.com/grego/blades-themes">themes repository</a>.</p>
</content>
        
    </entry>
    

    <entry>
        <title>Plugins</title>
        <link rel="alternate" href="/plugins/" />
        <id>/plugins</id>
        <summary></summary>
        <content><p>There are 4 types of plugins that can be used with Blades.</p>
<ul>
<li><strong>input</strong> - they can be used to get pages from different sources</li>
<li><strong>output</strong> - they can be used to generate further page data, such as processed images</li>
<li><strong>transform</strong> - they can transform anything on all of the pages</li>
<li><strong>content</strong> - they transform markdowned content of a particular page</li>
</ul>
<p>The plugins can be set up in the <a href="/config.html">config</a>.</p>
<p>Here is a list of known plugins. To contribute your own, submit a PR to the <a href="https://github.com/grego/blades-plugins">plugin repository</a>.</p>
</content>
        
    </entry>
    

    <entry>
        <title>KaTeX - Rust</title>
        <link rel="alternate" href="/plugins/katex-rs.html" />
        <id>/plugins/katex-rs</id>
        <summary>Render math formulas using KaTeX - written in Rust</summary>
        <content><p>Blades natively supports rendering LaTeX formulas into <a href="https://developer.mozilla.org/docs/Web/MathML">MathML</a>.
Sadly, Chromium-based browsers don’t support it.
This plugin renders the formulas into HTML using <a href="https://katex.org">KaTeX</a>,
run with the QuickJS enginge.
Formulas delimited by <code>$</code> are rendered in inline mode and by <code>$$</code> in display mode.</p>
<p>This plugin can be installed as</p>
<pre><code class="language-bash"><strong>cargo</strong> install blades-katex
</code></pre>
<p>Then, it can be used in Blades as</p>
<pre><code class="language-toml"><u>[</u><strong>plugins</strong>.<em>content</em><u>]</u>
<var>katex</var> = <span>&quot;blades-katex&quot;</span>
</code></pre>
<p>On each page where KaTeX should be rendered, it can be enabled by</p>
<pre><code class="language-toml"><var>plugins</var> = <u>[</u><span>&quot;katex&quot;</span><u>]</u>
</code></pre></content>
        
    </entry>
    

    <entry>
        <title>KaTeX - JavaScript</title>
        <link rel="alternate" href="/plugins/katex-js.html" />
        <id>/plugins/katex-js</id>
        <summary>Render math formulas using KaTeX - written in JavaScript</summary>
        <content><p>Blades natively supports rendering LaTeX formulas into <a href="https://developer.mozilla.org/docs/Web/MathML">MathML</a>.
Sadly, Chromium-based browsers don’t support it.
This plugin renders the formulas into HTML using <a href="https://katex.org">KaTeX</a>.
Formulas delimited by <code>$</code> are rendered in inline mode and by <code>$$</code> in display mode.</p>
<p>Be aware there is a <a href="/plugins/katex-rs.html">Rust version</a> that does the same,
but faster.</p>
<p>This plugin can be installed as</p>
<pre><code class="language-bash"><strong>yarn</strong> global add https://github.com/grego/blades-katex
</code></pre>
<p>Then, it can be used in Blades as</p>
<pre><code class="language-toml"><u>[</u><strong>plugins</strong>.<em>content</em><u>]</u>
<var>katex</var> = <span>&quot;blades-katex&quot;</span>
</code></pre>
<p>On each page where KaTeX should be rendered, it can be enabled by</p>
<pre><code class="language-toml"><var>plugins</var> = <u>[</u><span>&quot;katex&quot;</span><u>]</u>
</code></pre>
<p>This plugin is basically a stripped-down version of
<a href="https://katex.org/docs/autorender.html">auto-render extension</a> for KaTeX.</p>
</content>
        
    </entry>
    

    <entry>
        <title>casper</title>
        <link rel="alternate" href="/themes/casper.html" />
        <id>/themes/casper</id>
        <summary>A clean blog theme, loosely based on the old Ghost.io default, but without JavaScript</summary>
        <content><p>To use this theme, simply clone it into your <code>themes</code> directory:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> clone https://github.com/grego/casper
</code></pre>
<p>Or use it as a submodule:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> submodule add https://github.com/grego/casper
</code></pre>
<p>Then, set it in your <a href="/config.html">config</a>.</p>
<pre><code class="language-toml"><var>theme</var> = <span>&quot;casper&quot;</span>
</code></pre></content>
        
    </entry>
    

    <entry>
        <title>after-dark</title>
        <link rel="alternate" href="/themes/after-dark.html" />
        <id>/themes/after-dark</id>
        <summary>Secure and accessible dark theme</summary>
        <content><p>To use this theme, simply clone it into your <code>themes</code> directory:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> clone https://github.com/grego/after-dark
</code></pre>
<p>Or use it as a submodule:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> submodule add https://github.com/grego/after-dark
</code></pre>
<p>Then, set it in your <a href="/config.html">config</a>.</p>
<pre><code class="language-toml"><var>theme</var> = <span>&quot;after-dark&quot;</span>
</code></pre></content>
        
    </entry>
    

    <entry>
        <title>BLADE.386</title>
        <link rel="alternate" href="/themes/BLADE.html" />
        <id>/themes/BLADE</id>
        <summary>For people who like the MS-DOS æsthetics</summary>
        <content><p>To use this theme, simply clone it into your <code>themes</code> directory:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> clone https://github.com/grego/BLADE.386
</code></pre>
<p>Or use it as a submodule:</p>
<pre><code class="language-sh"><strong>cd</strong> themes
<strong>git</strong> submodule add https://github.com/grego/BLADE.386
</code></pre>
<p>Then, set it in your <a href="/config.html">config</a>.</p>
<pre><code class="language-toml"><var>theme</var> = <span>&quot;BLADE.386&quot;</span>
</code></pre></content>
        
    </entry>
    

</feed>